# SwiftFormat Configuration

# CRITICAL: Disable redundantSelf rule for Swift 6 strict concurrency compatibility
# Swift 6 requires explicit 'self.' in closures to make capture semantics clear
--disable redundantSelf

# Preserve existing formatting
--ifdef no-indent
--indentcase false
--extensionacl on-declarations

# Standard formatting
--indent 4
--maxwidth 140
--wraparguments before-first
--wrapparameters before-first
--wrapcollections before-first

# Disable trailing commas for Swift 5.9+ compatibility
# Trailing commas are only supported in Swift 6.1+
--trailingcommas never

